windows:
	key(win)
	
close (application | window):
	key(alt-space)
	key(c)
	
switch application:
	key(alt-tab)
	key(right)

switch to desktop:
	key(win-d)

# close all (applications | windows)

# windows (restart | reboot):
	
# windows shutdown:
# 	key(

windows control alt delete:
	key(ctrl-alt-delete)